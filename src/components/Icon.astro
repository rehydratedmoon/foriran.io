---
// export default ({ icon, ...props }) => {
//   const Icon = icon;
//   return <Icon class="icon mx-1 inline vertical-ac" height="0.8em" {...props} />;

// };

type Props = {
  name: string;
  class?: string;
  alt?: string;
};

const props = Astro.props;
---

<>
  {' '}
  <span class={`${props.name} inline-block vertical-text-top ${props.class ?? ''}`}>
    <slot />
  </span>
  {' '}
</>
